numbers = []
minnum = 1000
while input("stop?") != "yes":
    numbers.append(int(input("input number")))

for i in range(len(numbers)):
    for j in range(len(numbers)):
        if(numbers[i] - numbers[j]) < minnum:
            minnum = numbers[i] - numbers[j]
